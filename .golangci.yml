linters:
  enable:
    - errcheck
    - goconst
    - revive
    - gosec
    - govet
    - ineffassign
    - lll
    - gosimple
    - staticcheck
    - misspell
    - unconvert
    - unused
  enable-all: false
issues:
  exclude-files:
    - schema.go
    - pulumiManifest.go
linters-settings:
  lll:
    line-length: 150
run:
  timeout: 10m
