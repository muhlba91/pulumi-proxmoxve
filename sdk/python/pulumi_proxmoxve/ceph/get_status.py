# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = [
    'GetStatusResult',
    'AwaitableGetStatusResult',
    'get_status',
    'get_status_output',
]

@pulumi.output_type
class GetStatusResult:
    """
    A collection of values returned by getStatus.
    """
    def __init__(__self__, fsid=None, health_status=None, id=None, node_name=None, quorum_names=None):
        if fsid and not isinstance(fsid, str):
            raise TypeError("Expected argument 'fsid' to be a str")
        pulumi.set(__self__, "fsid", fsid)
        if health_status and not isinstance(health_status, str):
            raise TypeError("Expected argument 'health_status' to be a str")
        pulumi.set(__self__, "health_status", health_status)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if node_name and not isinstance(node_name, str):
            raise TypeError("Expected argument 'node_name' to be a str")
        pulumi.set(__self__, "node_name", node_name)
        if quorum_names and not isinstance(quorum_names, list):
            raise TypeError("Expected argument 'quorum_names' to be a list")
        pulumi.set(__self__, "quorum_names", quorum_names)

    @_builtins.property
    @pulumi.getter
    def fsid(self) -> _builtins.str:
        """
        Ceph cluster UUID.
        """
        return pulumi.get(self, "fsid")

    @_builtins.property
    @pulumi.getter(name="healthStatus")
    def health_status(self) -> _builtins.str:
        """
        Overall cluster health: `HEALTH_OK`, `HEALTH_WARN`, or `HEALTH_ERR`.
        """
        return pulumi.get(self, "health_status")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The Ceph cluster `fsid`.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="nodeName")
    def node_name(self) -> Optional[_builtins.str]:
        """
        Optional node name. When set, the data source queries the per-node endpoint; when omitted, it queries the cluster-wide endpoint.
        """
        return pulumi.get(self, "node_name")

    @_builtins.property
    @pulumi.getter(name="quorumNames")
    def quorum_names(self) -> Sequence[_builtins.str]:
        """
        Monitor names currently participating in the quorum.
        """
        return pulumi.get(self, "quorum_names")


class AwaitableGetStatusResult(GetStatusResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetStatusResult(
            fsid=self.fsid,
            health_status=self.health_status,
            id=self.id,
            node_name=self.node_name,
            quorum_names=self.quorum_names)


def get_status(node_name: Optional[_builtins.str] = None,
               opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetStatusResult:
    """
    > **Experimental.** Schema and behavior may change in future releases. Pin the provider version if stability matters.

    Retrieves Ceph status from Proxmox. Queries the per-node endpoint when `node_name` is set, otherwise the cluster-wide endpoint.

    When `node_name` is set, the data source queries `/nodes/{node}/ceph/status`
    (the node-local view). When `node_name` is omitted, it queries
    `/cluster/ceph/status` (the cluster-wide view). Both endpoints return the
    same payload shape; only the failure modes differ when Ceph is misconfigured
    on a specific node.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_proxmoxve as proxmoxve

    # Cluster-wide Ceph status (hits GET /cluster/ceph/status).
    cluster = proxmoxve.ceph.get_status()
    # Node-scoped Ceph status (hits GET /nodes/{node}/ceph/status).
    node = proxmoxve.ceph.get_status(node_name="pve")
    pulumi.export("cephHealth", cluster.health_status)
    pulumi.export("cephFsid", cluster.fsid)
    pulumi.export("cephQuorum", cluster.quorum_names)
    ```


    :param _builtins.str node_name: Optional node name. When set, the data source queries the per-node endpoint; when omitted, it queries the cluster-wide endpoint.
    """
    __args__ = dict()
    __args__['nodeName'] = node_name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('proxmoxve:ceph/getStatus:getStatus', __args__, opts=opts, typ=GetStatusResult).value

    return AwaitableGetStatusResult(
        fsid=pulumi.get(__ret__, 'fsid'),
        health_status=pulumi.get(__ret__, 'health_status'),
        id=pulumi.get(__ret__, 'id'),
        node_name=pulumi.get(__ret__, 'node_name'),
        quorum_names=pulumi.get(__ret__, 'quorum_names'))
def get_status_output(node_name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetStatusResult]:
    """
    > **Experimental.** Schema and behavior may change in future releases. Pin the provider version if stability matters.

    Retrieves Ceph status from Proxmox. Queries the per-node endpoint when `node_name` is set, otherwise the cluster-wide endpoint.

    When `node_name` is set, the data source queries `/nodes/{node}/ceph/status`
    (the node-local view). When `node_name` is omitted, it queries
    `/cluster/ceph/status` (the cluster-wide view). Both endpoints return the
    same payload shape; only the failure modes differ when Ceph is misconfigured
    on a specific node.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_proxmoxve as proxmoxve

    # Cluster-wide Ceph status (hits GET /cluster/ceph/status).
    cluster = proxmoxve.ceph.get_status()
    # Node-scoped Ceph status (hits GET /nodes/{node}/ceph/status).
    node = proxmoxve.ceph.get_status(node_name="pve")
    pulumi.export("cephHealth", cluster.health_status)
    pulumi.export("cephFsid", cluster.fsid)
    pulumi.export("cephQuorum", cluster.quorum_names)
    ```


    :param _builtins.str node_name: Optional node name. When set, the data source queries the per-node endpoint; when omitted, it queries the cluster-wide endpoint.
    """
    __args__ = dict()
    __args__['nodeName'] = node_name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('proxmoxve:ceph/getStatus:getStatus', __args__, opts=opts, typ=GetStatusResult)
    return __ret__.apply(lambda __response__: GetStatusResult(
        fsid=pulumi.get(__response__, 'fsid'),
        health_status=pulumi.get(__response__, 'health_status'),
        id=pulumi.get(__response__, 'id'),
        node_name=pulumi.get(__response__, 'node_name'),
        quorum_names=pulumi.get(__response__, 'quorum_names')))
